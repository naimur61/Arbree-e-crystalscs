echo "🚀 Running pre-push checks..."

# Full TypeScript check (entire project)
npx tsc --noEmit

# Full ESLint check (entire project)
npx eslint src/ --max-warnings=0 --format codeframe

# Production build (catch build errors early)
npx next build

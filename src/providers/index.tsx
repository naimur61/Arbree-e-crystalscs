'use client';

/* ── Providers: placeholder — uncomment AuthProvider wrapper when backend is ready ── */

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
